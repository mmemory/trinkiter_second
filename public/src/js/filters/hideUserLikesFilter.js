trinkiter.filter('hideUserLikes', function() {

    return function(items, userId) {
        var returnArray = [];
        if(items) {

            angular.forEach(items, function(item) {
                //if(indexOf())
            });
        }
    }
});